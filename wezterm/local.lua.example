-- Copy to ~/.config/wezterm/local.lua (untracked). Keys replace the values
-- set in wezterm.lua; color_scheme also switches the time-based schedule off.
return {
    -- font_size = 13,
    -- window_background_opacity = 1.0,
    -- color_scheme = "Catppuccin Mocha",
}
